<!--from https://habr.com/ru/post/29694/ -->
<hibernate-mapping>
    <!-- Тег class имеет два параметра: параметр name — Имя класса (необходимо указывать полный путь
    с учетом структуры пакетов) и параметр table — имя таблицы в базе данных, на которую будет маппиться наш класс. -->
    <class name="hibernatePark.model_origin.Route" table="route">
        <!-- Тег id описывает идентификатор. Параметр column указывает на какую колонку в таблице будет ссылаться
         поле id нашего объекта, так же указываем класс и указываем generator, который отвечает за генерацию id. -->
        <id column="route_id" name="id" type="java.lang.Long">
            <generator class="increment"/>
        </id>
        <!-- property описывает простое поле нашего объекта, в качестве параметров указываем имя поля,
         его класс и имя колонки в таблице. -->
        <property column="name" name="name" type="java.lang.String"/>
        <property column="number" name="number" type="java.lang.Integer"/>
        <!-- set описывает поле в котором содержится некий набор(коллекция) объектов. -->
        <!-- параметр name — имя поля нашего объекта, параметр table — имя таблицы связи
        (в случае отношения многие ко многим) и параметр lazy.  false, то у нас при получении объекта Route
        из базы вместе с объектом достается и коллекция объектов Bus, так как busses это поле объекта Route.
        А если в качестве параметра мы указываем значение true, то коллекция объектов Bus не вытаскивается,
        для ее получения надо явно вызывать метод route.getBusses(). -->
        <set name="busses" lazy="false">
            <!-- key имеет параметр column, который говорит, на какую колонку в таблице связи будет ссылаться поле нашего объекта. -->
            <key column="route_id"/>
            <!-- many-to-many описывает связь типа многие ко многим, в качестве параметров тег использует column-имя
            колонки второй колонки в таблице связи и параметр class, указывающий какого класса будут объеты на той стороне. -->
            <one-to-many class="hibernatePark.model.Bus"/>
        </set>

    </class>
</hibernate-mapping>